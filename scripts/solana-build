#!/bin/bash
set -ax
. ../sdb_sol.ini

cd ../solana
/bin/bash sdk/docker-solana/build.sh
docker push ${SDB_SOL_DOCKER}

# docker tag ${SDB_SOL_DOCKER} ${SDB_SOL_DOCKER_IMG}:latest
# docker push ${SDB_SOL_DOCKER_IMG}:latest
set +ax
