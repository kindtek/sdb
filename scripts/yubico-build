#!/bin/bash
set -ax
./docker-login
cd ../yubico-net-sdk
. sdb.ini
set +a

cd Yubico.NativeShims
/bin/bash build-${YUBICO_BUILD_ENV}.sh

docker tag  "${SDB_YUB_DOCKER}"  "${SDB_YUB_DOCKER_IMG}:${SDB_YUB_DOCKER_TAG2}"
docker push "${SDB_YUB_DOCKER_IMG}"